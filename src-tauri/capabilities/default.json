{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the picker and per-repo windows",
  "windows": ["picker", "repo-*"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default"
  ]
}
